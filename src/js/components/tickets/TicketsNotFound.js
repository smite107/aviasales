import React from "react";

const TicketsNotFound = () => (
  <div className="ticket-list__not-found">
    Извините, к сожалению, билетов, подходящих под ваши критерии, не найдено :(
  </div>
);

export default TicketsNotFound;